{
  "name": "stocklive-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd client && rm -rf node_modules package-lock.json && npm install --force && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@supabase/supabase-js": "^2.39.3",
    "node-cache": "^5.1.2",
    "dotenv": "^16.4.1",
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}